import Cabecera from "./cabecera"

const Login = (user, password) => {

    return(
        <Container>
            {
                user == 'a' ?
                <Cabecera>
            
                </Cabecera>
        : 'error en el login'
            }
        </Container>
    )
}
export default Login